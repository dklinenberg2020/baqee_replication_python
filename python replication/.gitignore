__pycache__/
# Raw OECD ICIO year files (e.g. 2022_SML.csv) are too large for a normal
# GitHub push -- ignored by year-prefixed name, not by extension, so small
# committed CSVs like fontagne_icio_trade_elast.csv aren't caught too.
baqaee_farhi_model/[0-9][0-9][0-9][0-9]*.csv
